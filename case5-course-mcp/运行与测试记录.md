# 作业5运行与测试记录

## 已完成内容

- 已创建 SQLite 数据库 `courses.db`。
- 已创建 `courses`、`teachers`、`assignments` 三张表，并写入示例课程数据。
- 已使用 MCP Python SDK 的 `FastMCP` 封装 5 个 MCP Tools。
- 已提供 Codex MCP 配置文件，并已将配置加入本机 `~/.codex/config.toml`。

## MCP Tools

| 工具 | 验证结果 |
| --- | --- |
| `list_courses` | 可按课程、教师和学院查询 |
| `get_course` | `CS205` 返回“数据库系统”及教师、时间信息 |
| `list_teachers` | 可按教师和学院查询 |
| `list_assignments` | `CS205` 返回“课程数据库设计”作业 |
| `query_course_information` | “张伟老师教什么课”返回 CS101、CS205 |

## 执行命令与结果

```bash
uv sync
uv run python demo.py
uv run pytest -q
```

测试结果：`4 passed in 0.37s`。

真实 MCP stdio 测试会启动 `server.py`、初始化 MCP Client、读取工具列表，并调用：

```text
get_course({"course_code": "CS205"})
```

返回内容包含“数据库系统”，说明 Server 的工具注册和调用链路正常。

## 运行截图

`screenshots/终端真实运行截图.png` 为在 macOS Terminal 中实际执行以下命令后截取的桌面截图，画面包含课程详情、作业查询结果及 `4 passed` 测试结论：

```bash
./.venv/bin/python demo.py && ./.venv/bin/python -m pytest -q
```

`screenshots/功能运行截图.png` 同时保留为便于阅读的运行输出图。
