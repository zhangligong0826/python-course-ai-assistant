a7 带引用对话验证（DeepSeek + 知识库 RAG）
执行时间：2026-09-04 09:57-10:00
实例：127.0.0.1:8083（隔离 KB 实例），模型：course-python-assistant（基座 deepseek-v4-flash）
知识库：Python 程序设计课程知识库（bge-m3 嵌入索引）
链路：/api/chat/completions + files[type=collection] → RAG 注入 → DeepSeek 生成
==============================================================================
请求：函数的默认参数和返回值怎么写？
结果：HTTP 200
引用：citations=1，source="Python 程序设计课程知识库"，documents=3（行内引用 [1]）
回答（节选）：结论：函数的默认参数是在定义函数时给参数一个预设值；函数的返回值
通过 `return` 语句指定。如果没有 `return`，函数默认返回 `None`。另外，默认参数应
避免使用可变对象（如列表），否则会在多次调用间共享状态 [1]。
判定：PASS（引用注入 + 行内标注 + 内容与 03-functions.md 一致）
==============================================================================
配置说明：
- 课程模型挂载 meta.knowledge；API 调用时显式传 files(type=collection) 走标准注入链路。
- 密钥从原库读取，仅内存注入连接配置，未打印、未写入仓库。
