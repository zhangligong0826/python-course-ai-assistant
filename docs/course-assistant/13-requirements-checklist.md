# 课程开发要求逐条对照自查表

**核验日期：** 2026-09-04
**核验方式：** 资料目录实测统计 + 隔离实例（8083）API 实测 + 数据库直读核验
**结论：** 六项要求全部满足

| # | 开发要求 | 结论 | 核验证据 |
| --- | --- | --- | --- |
| 1 | 不少于 8 个资料文件，或资料总量不少于 30 页 | 满足 | 仅 AIOps 方向即有 **11 个资料文件**（`course-materials/aiops/`，按"或"条件单独达标）；全库 22 个 Markdown + 示例代码，合计约 2.2 万字（按 A4 每页 600–700 字估算约 30+ 页） |
| 2 | 资料按章节或知识模块分类 | 满足 | AIOps：01–10 按知识模块编号（AIOps 基础 / SRE 可观测性 / LLM4Ops / 模型选型 / 事故管理 / 异常检测 / 根因分析 / 大模型基础 / 工具生态 / 故障案例），配 README 索引表；Python 方向 01–06 按章节，`materials-manifest.csv` 登记章节与类型 |
| 3 | 至少包含 3 类资料（如课件、实验指导、示例代码） | 满足（4 类） | manifest 统计：**lecture 课件 6 份、lab_guide 实验指导 2 份、example_code 示例代码 1 份、error_reference 错误参考 1 份**；AIOps 方向另含案例演练库、模型数据卡 |
| 4 | AI 回答课程问题时优先依据知识库 | 满足 | 课程模型挂载"AIOps 智能运维知识库"（meta.knowledge 实测核验）；系统提示词明确"回答运维问题时优先依据提供的知识库资料"；三张快捷卡片提示词均要求"先检索知识库再回答" |
| 5 | 回答中给出资料来源或引用依据 | 满足 | citations 能力已开启，UI 显示 Citation 徽标并可点开看来源；实测对话 citations=1；定向检索 11/11 命中（见 `evidence/aiops-rag-retrieval.txt`、`evidence/aiops-citation-chat.txt`） |
| 6 | 知识库没有的信息明确说明"不确定/资料中未找到"，不得编造 | 满足 | 系统提示词约束"资料中没有的内容要明确说明超出资料范围"；实测提问库外问题"如何配置 Zabbix 邮件告警"，回答明示"提供的知识库资料不包含 Zabbix 邮件告警的具体配置方法……无法给出带资料依据的配置步骤"，通用经验部分标注"仅供参考"，未伪装成资料依据 |

## 补充证据

- 历史演示 T14（超课程范围提示）、T15（拒绝代写）覆盖第 6 条场景，会话持久化于隔离实例侧边栏（摘要见 `evidence/2026-09-04-15-scenarios.md`，15/15 通过）。
- 第 6 条补充约束：高风险运维操作只给只读排查建议，不得直接建议未经审批的写操作（系统提示词 + 知识库《大模型用于 AIOps》执行边界）。

## 功能需求逐条自查（第二组：教学能力 8 项）

**核验日期：** 2026-09-04（全部为当日 API/服务实测）

| # | 功能需求 | 结论 | 实测证据 |
| --- | --- | --- | --- |
| 1 | 课程知识问答 | 满足 | 知识库挂载模型实测：SLO/错误预算、告警风暴等问答均命中资料并带引用；定向检索 11/11 |
| 2 | 对知识点进行通俗解释 | 满足 | 实测"用大白话解释错误预算"→"错误预算就是允许出错的额度"，配生活化例子 |
| 3 | 根据学生水平调整回答难度 | 满足 | 实测"零基础解释告警风暴"→自动切换为"大楼停电"日常类比；历史演示 T13 同项通过 |
| 4 | 给出例题、示例代码或解题思路 | 满足 | 实测"PromQL 计算 5xx 错误率"→给出例题、参考答案与解题思路（资料引用）；Python 方向含示例代码资料 |
| 5 | 根据指定章节生成练习题 | 满足 | 题库工具实测：`chapter=07/08`（AIOps 章节）生成成功返回 quiz_id；模型端到端调用成功（"生成 07 章 2 道 beginner 单选"→返回 MTTA、黄金信号题目） |
| 6 | 对学生答案进行分析并提供改进建议 | 满足 | 判分工具实测：返回得分（2/2）、逐题 correct/explanation/concept/source；模型侧输出成绩卡与复习建议（历史 T10/T11 通过） |
| 7 | 提供知识来源或引用 | 满足 | citations 能力开启；实测多轮回答 citations=1；UI Citation 徽标可点开查看来源片段 |
| 8 | 对超出课程范围的问题进行提示 | 满足 | 实测库外问题"如何配置 Zabbix 邮件告警"→明示"知识库资料不包含…无法给出带资料依据的配置步骤"，通用内容标注"仅供参考"；历史 T14/T15 同项通过 |

### 第 5 项配套修复记录（2026-09-04）

- 隔离实例（8083）此前未注册题库/检索工具服务 → 已通过 `POST /api/v1/configs/tool_servers` 注册 `server:python-course-quiz`（8091）与 `server:python-course-rag`（8092），并配置全员读授权。
- 课程模型补充 `meta.toolIds` 挂载两个工具，前端"自测练习"卡片开箱即用。
- 题库章节枚举为 `01`–`06`（Python）→ 新增 **`07` AIOps 基础与可观测性、`08` 大模型与智能运维** 各 6 题（题库共 60 题），来源指向 `course-materials/aiops/` 资料；快捷卡片提示词同步改为"章节：07"。

## 复验方法

```bash
# 1. 资料数量与类型（要求 1/2/3）
ls course-materials/aiops/
cut -d, -f2 course-materials/python-programming/materials-manifest.csv | sort | uniq -c

# 2. 模型挂载与系统提示词（要求 4）
sqlite3 /private/tmp/kb-e2e/webui.db "SELECT params FROM model WHERE id='course-aiops-assistant';"

# 3. 引用与拒答实测（要求 5/6）：登录 http://127.0.0.1:5174 选择"AIOps 智能运维助教"
#    引用：问"什么是 SLO 和错误预算？" → 查看回答下方 Citation
#    拒答：问"如何配置 Zabbix 的邮件告警？" → 应回答资料中未找到
```
