# Codex 交互记录（会话摘要）

记录范围：课程 AI 助教项目全周期（W101–W605）与 AI 编码代理（Codex/Trae）的关键交互。
记录方式：按任务阶段归档关键指令与代理产出；完整原始对话见各任务文档与证据文件。

## 2026-09-03 部署与链路（W101–W104）

- 指令：基于 open-webui 项目部署课程专属 AI 助手，先做仓库审计与部署基线。
- 代理产出：仓库审计（00）、隔离数据目录部署基线（01）、DeepSeek 连接验证（02）。
- 关键决策：临时 DATA_DIR + 显式 DATABASE_URL；BYPASS_EMBEDDING_AND_RETRIEVAL 仅限部署基线。

## 2026-09-03 工具与验收准备（W301–W501）

- 指令：实现题库抽题/判分工具并以 OpenAPI Tool Server 接入；准备 15 条固定验收集。
- 代理产出：course-assistant-tool（16/16 测试通过）、v1/v2 系统提示词（04/06）、固定测试集（05）、验收矩阵（acceptance-matrix.txt）。

## 2026-09-04 RAG 阻塞恢复与优化（W205，方案 a 执行）

- 指令：按方案（a）执行——恢复 Ollama 嵌入，导入资料，六章定向检索，保存证据。
- 交互轮次（关键）：
  1. 上传竞态导致内容为空 → 代理改为同步上传 + 轮询，12/12 成功。
  2. 六章检索 5/7 → 代理开启混合检索重跑，仍 5/7。
  3. 要求分析未命中原因 → 代理定位三层根因（无重排时排序由嵌入决定 / BM25 单字符切分 / nomic 中文语义弱），写入 08。
  4. 批准切换 bge-m3 → 重建索引，7/7 通过。
  5. 引用验证首轮 citations=0 → 代理定位 API 注入路径，显式 files[type=collection] 后返回行内引用 [1]。

## 2026-09-04 提交与收尾（W604–W605）

- 指令：撰写执行报告、整理交互记录、Git 提交。
- 代理产出：09-execution-report.md（本报告）、logs/codex-interaction-log.md、决策记录更新、Git 提交。

## 使用心得

- 阻塞先留痕（03-rag-blocker.md），恢复后补"解除记录"，避免伪造通过结论。
- 检索类问题先做根因分层（排序链路 / 分词 / 模型能力 / 测试集设计），再决定改动点，避免盲目调参。
- 一切优化结论以同一查询集重跑对比为准，基线证据保留。
